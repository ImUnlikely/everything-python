import numpy as np # import library with variable name

nums = [100, 200, 300]
print(np.mean(nums))

with open(r"keywords\textfile.txt", mode="w") as f: # open files with variable name
    f.write("Write this text to textfile!")
    
